$(document).ready(function(){
  $('.datepicker').datepicker({
    format: "yyyy"
  });
  $('.gestion .datepicker').datepicker({
    format: "mm-yyyy"
  });
  $('.experienciaP .datepicker').datepicker({
    format: "mm-yyyy"
  });

})
